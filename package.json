{
  "name": "huddle01-extension-ts",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-extension": "yarn build && @powershell copy 'src/manifest.json' 'dist/'"
  },
  "dependencies": {
    "preact": "^10.5.15"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.1.5",
    "typescript": "^4.5.4",
    "vite": "^2.9.7"
  }
}